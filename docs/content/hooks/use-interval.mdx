# useInterval

## Purpose

Run a callback repeatedly at a fixed delay.

## Import

~~~tsx
import { useInterval } from "@tesseract-nexus/tesserix-ui"
~~~

## Example

~~~tsx
useInterval(() => setTick((n) => n + 1), 1000)
~~~
