# Tour

## Overview

Guided onboarding flow rendered as a modal overlay with step navigation.

## Import

~~~tsx
import { Tour } from "@tesseract-nexus/tesserix-ui"
~~~

## Usage

~~~tsx
<Tour open={open} onOpenChange={setOpen} steps={steps} />
~~~
